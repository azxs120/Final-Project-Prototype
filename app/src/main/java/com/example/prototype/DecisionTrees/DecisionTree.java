package com.example.prototype.DecisionTrees;

import com.example.prototype.DBClasess.Apartment;

import java.util.ArrayList;

public class DecisionTree {
   /* ArrayList<Apartment> apartments = new ArrayList<>();
    BineryTree rootNode = null;


    String[] q = {"Air Conditioning", "Balcony", "Bars",
            "Disabled Access", "Elevator", "Furnished", "Panic Room", "Parking",
            "Pets", "Renovated"};
    //private static int id = 0;

    public DecisionTree(ArrayList<Apartment> apartments) {
        this.apartments = apartments;
        //id++;
        createRoot("Air Conditioning");
        createTree(1, rootNode);
    }

    private void createTree(int i, BineryTree node) {
        if (i >= q.length) {
            return;
        }
        //yes
        node.setYesBranch(new BineryTree(q[i]));
        if(apartments.get(0).getQ().get(i).equals(true));
        createTree(i + 1, node.getYesBranch());

        //no
        node.setNoBranch(new BineryTree(q[i]));
        createTree(i + 1, node.getNoBranch());
    }

    private void createRoot(String newQuestAns) {
        rootNode = new BineryTree(newQuestAns);
    }

    public ArrayList<Apartment> getMatch() {
        search(rootNode, 0, 0);
    }

    private boolean search(BineryTree rootNode, int i, int j) {
        if(i >= apartments.size())
            return;
        if(j >= 10)
            return;
        if (apartments.get(i).getQ().get(j) == true)

                else

    }

}
        return true;

                }*/
                }

